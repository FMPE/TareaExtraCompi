# Opcional: URL absoluta del backend (sin / al final).
# Vacío o ausente = mismo origen que la web (recomendado con Docker + Nginx o proxy de CRA).
# Ejemplo producción separada:
# REACT_APP_API_URL=https://tu-servicio-api.onrender.com
